# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Runtime artifacts generated by maintest.py (regenerated each run)
loan_calc_debug.json

# Node / TypeScript leftovers (project is Python-only now)
/node_modules
npm-debug.log*
package-lock.json

# REAL identity data for --kyc auto/ovd (real PAN / Aadhaar / OVD numbers and scans).
# Never commit these. config/kyc_identity.example.json is the safe template and IS tracked.
/config/*
!/config/kyc_identity.example.json

# Editor / OS junk
.vscode/*
.idea/
.DS_Store
