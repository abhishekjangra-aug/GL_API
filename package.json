{
  "name": "augmont-gold-loan-api-test",
  "version": "1.0.0",
  "private": true,
  "description": "Node dependency for the Python harness: crypto-js provides the CryptoJS-AES encryption the Augmont backend expects for identity-proof fields (called via `node` from src/maintest.py).",
  "dependencies": {
    "crypto-js": "^4.2.0"
  }
}
